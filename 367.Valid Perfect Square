class Solution:
    def isPerfectSquare(self, num):
        low,high=1,num
        while (low<=high):
            mid=(low+high)//2
            sq=mid*mid
            if (sq==num):
                return True
            if (sq<num):
                low=mid+1
            else:
                high=mid-1
        return False
   

        
